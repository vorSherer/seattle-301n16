/*

const express = require('express');
const app = express();
app.get('/', (req,res) => {
})
app.get('/data', (req,res) => {})
app.listen(3000);

Make an express server with 2 routes

one is a / that says hello using an .ejs file

one is a /data that sends out some JSON (fake if you want)

*/